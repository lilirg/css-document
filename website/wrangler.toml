name = "css-document"
compatibility_date = "2024-01-01"

[site]
bucket = "./build"

# 自定义域名配置（可选）
# routes = [
#   { pattern = "docs.yourdomain.com", custom_hostname = true }
# ]

# 环境变量（可选）
# [vars]
# MY_VARIABLE = "production_value"